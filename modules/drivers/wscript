def set_options(opt):
    opt.sub_options('pcre fftw zlib uuid expat sql')

def configure(conf):
    conf.sub_config('pcre fftw zlib uuid expat sql')

def build(bld):
    bld.add_subdirs('pcre fftw zlib uuid expat')

def distclean(ctx):
    ctx.recurse('pcre fftw zlib uuid expat')
