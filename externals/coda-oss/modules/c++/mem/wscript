NAME            = 'mem'
VERSION         = '1.0'
MODULE_DEPS     = 'sys gsl std'
TEST_DEPS       = 'cli std'

options = configure = distclean = lambda p: None

def build(bld):
    bld.module(**globals())
